name: Dispatch work flow
on:
  workflow_dispatch
jobs:
  Dispatch:
    runs-on: 	
      - self-hosted
      - Windows
      - test
    steps:
     - name: Auto Build
       run: |
            echo "auto build"
    
    
