name: Atlas Project Binaries
run-name: Building Atlas Project Binaries
on:
  pull_request:
    branches:
      - main
    types:
      - closed
jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: 	
      - self-hosted
      - Windows
      - test
    env:
      AUTHOR_NAME: ${{github.actor}}
    steps:
     - uses: actions/checkout@v4
       with:
          clean: true
          fetch-depth: '0'
          submodules: 'true'
          token: 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   
     - name: Build
       run: |
            echo "Build new binaries"
            .\commit.bat 
    
    
