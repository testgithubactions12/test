name: Scheduler
on:
  schedule:
    - cron: '* * * * *'
jobs:
  scheduled_run:    
    runs-on: 	
      - ubuntu-latest
    steps:
      - name: jop
        run: |
             echo "scheduled actions"

    
    
